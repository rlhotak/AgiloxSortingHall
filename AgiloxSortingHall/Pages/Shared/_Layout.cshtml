<!DOCTYPE html>
<html lang="en" class="dark-mode">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - AgiloxSortingHall</title>
    <script type="importmap"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/AgiloxSortingHall.styles.css" asp-append-version="true" />
</head>
<body class="@ViewData["BodyClass"]">
    <div class="container">
        <main role="main">
            @RenderBody()
        </main>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.5/signalr.min.js"
            integrity="sha256-..."
            crossorigin="anonymous"></script>

    <script>
                document.addEventListener("submit", function () {
            window.isNavigating = true;
        });

        const connection = new signalR.HubConnectionBuilder()
            .withUrl("/hallHub")
            .withAutomaticReconnect()
            .build();

               connection.on("HallUpdated", () => {
                        if (window.isNavigating) {
            console.debug("HallUpdated ignored – navigation in progress");
            return;
        }

            const path = window.location.pathname.toLowerCase();

            // Reload jen na hlavní přehled + případně skladníka
            if (path === "/" ||
                path === "/index" ||
                path === "/index/" ||
                path.startsWith("/skladnik") ||
                path.startsWith("/stul"))
            {
                location.reload();
            }
        });


        connection.start().catch(err => console.error(err.toString()));
    </script>



    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
